#weather application
built a simple weather service system using html, css and JavaScript
--used two sets of api
--1 used a location api to get the longitude and latitude of a location
--2 used the weather api to get the weather information of the location
#They system takes a users location and get the current weather of the location based on the users input
--if the location does not exist or the user mispelled the location the application will inform the user
